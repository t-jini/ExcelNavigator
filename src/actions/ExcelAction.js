export const ListenWorkbook = (state) => ({
  type: 'LISTEN_WORKBOOK',
  state
})

export const ListenWorksheet = (state) => ({
  type: 'LISTEN_WORKSHEET',
  state
})

export const AddTablesAndCharts = (state) => ({
  type: 'ADD_TABLESANDCHARTS',
  state
})

export const LoadWorkbook = (state) => ({
  type: 'LOAD_WORKBOOK',
  state
})

export const ActivateWorksheet = (state) => ({
  type: 'ACTIVATE_WORKSHEET',
  state
})

export const DeleteWorksheet = (state) => ({
  type: 'DELETE_WORKSHEET',
  state
})

export const ColoredRange = (state) => ({
  type: 'COLORED_RANGE',
  state
})
